# Vulnerable example

An intentionally unsafe agent workspace used as a detection fixture. Nothing in
here is executable: the files are configuration and prose only, and the strings
that look like credentials are fake.

```
toolward scan examples/vulnerable
```
